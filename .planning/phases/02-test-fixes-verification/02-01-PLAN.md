---
phase: 02-test-fixes-verification
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/models/test_category_model.py
  - tests/unit/models/test_customer_model.py
  - tests/unit/models/test_employee_model.py
  - tests/unit/models/test_discount_model.py
  - tests/unit/models/test_item_model.py
  - tests/unit/models/test_payment_type_model.py
  - tests/unit/models/test_receipt_model.py
  - tests/unit/models/test_pos_device_model.py
autonomous: true
requirements:
  - TST-01
  - TST-02
  - VER-01

must_haves:
  truths:
    - "All 8 test model files import from loyverse_sdk (not loyverse_api)"
    - "Receipt model test uses correct surcharge field name (not typo surchage)"
    - "pytest exits with zero failures and zero errors"
  artifacts:
    - path: "tests/unit/models/test_category_model.py"
      provides: "Category model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_customer_model.py"
      provides: "Customer model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_employee_model.py"
      provides: "Employee model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_discount_model.py"
      provides: "Discount model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_item_model.py"
      provides: "Item model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_payment_type_model.py"
      provides: "Payment type model unit test with correct import"
      contains: "from loyverse_sdk.models"
    - path: "tests/unit/models/test_receipt_model.py"
      provides: "Receipt model unit test with correct import and surcharge field"
      contains: "from loyverse_sdk.models"
      contains: "r.surcharge"
    - path: "tests/unit/models/test_pos_device_model.py"
      provides: "POS device model unit test with correct import"
      contains: "from loyverse_sdk.models"
  key_links:
    - from: "8 test files"
      to: "loyverse_sdk models"
      via: "import statement"
      pattern: "from loyverse_sdk.models"
---

<objective>
Fix all test import errors and verify tests pass.

Purpose: Fix 8 unit test files that incorrectly import from `loyverse_api` instead of `loyverse_sdk`, correct the `surchage` typo to `surcharge` in the receipt model test, and confirm all tests pass.

Output: 8 corrected test files + pytest verification output
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-fixes-verification/02-CONTEXT.md

# Source of truth for receipt model surcharge field
src/loyverse_sdk/models/receipt.py:84: surcharge: NonNegativeFloat = 0.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix import statements in 7 test files (excluding receipt test)</name>
  <files>tests/unit/models/test_category_model.py
tests/unit/models/test_customer_model.py
tests/unit/models/test_employee_model.py
tests/unit/models/test_discount_model.py
tests/unit/models/test_item_model.py
tests/unit/models/test_payment_type_model.py
tests/unit/models/test_pos_device_model.py</files>
  <action>
For each of the 7 files listed in files_modified, read the file and replace the incorrect import:
- OLD: `from loyverse_api.models import ...`
- NEW: `from loyverse_sdk.models import ...`

Specifically:
- test_category_model.py line 6: `from loyverse_api.models` → `from loyverse_sdk.models`
- test_customer_model.py line 6: `from loyverse_api.models` → `from loyverse_sdk.models`
- test_employee_model.py line 7: `from loyverse_api.models` → `from loyverse_sdk.models`
- test_discount_model.py lines 7-8: `from loyverse_api.models...` → `from loyverse_sdk.models...`
- test_item_model.py line 6: `from loyverse_api.models` → `from loyverse_sdk.models`
- test_payment_type_model.py line 6: `from loyverse_api.models` → `from loyverse_sdk.models`
- test_pos_device_model.py line 5: `from loyverse_api.models` → `from loyverse_sdk.models`

Use the edit tool with the exact oldString/newString for each file. Do NOT use replaceAll — fix only the incorrect `loyverse_api` import in each file.
  </action>
  <verify>
<automated>grep -l "from loyverse_api" tests/unit/models/test_category_model.py tests/unit/models/test_customer_model.py tests/unit/models/test_employee_model.py tests/unit/models/test_discount_model.py tests/unit/models/test_item_model.py tests/unit/models/test_payment_type_model.py tests/unit/models/test_pos_device_model.py 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 7 files contain `from loyverse_sdk.models` and no file contains `from loyverse_api`</done>
</task>

<task type="auto">
  <name>Task 2: Fix import and typo in receipt model test</name>
  <files>tests/unit/models/test_receipt_model.py</files>
  <action>
In tests/unit/models/test_receipt_model.py:
1. Fix the import on line 6: `from loyverse_api.models` → `from loyverse_sdk.models`
2. Fix the typo on line 140: change `r.surchage` → `r.surcharge`

Use the edit tool with exact oldString/newString:
- Edit 1: replace `from loyverse_api.models` with `from loyverse_sdk.models` in the import line
- Edit 2: replace `.surchage` with `.surcharge` on line 140

Verify line 140 uses `r.surcharge` (correct field name per src/loyverse_sdk/models/receipt.py:84).
  </action>
  <verify>
<automated>grep -c "from loyverse_api" tests/unit/models/test_receipt_model.py && grep -c "surchage" tests/unit/models/test_receipt_model.py && echo "0 errors in receipt test"</automated>
  </verify>
  <done>test_receipt_model.py imports from loyverse_sdk and uses r.surcharge (not r.surchage)</done>
</task>

<task type="auto">
  <name>Task 3: Verify all tests pass</name>
  <files>tests/unit/models/test_category_model.py
tests/unit/models/test_customer_model.py
tests/unit/models/test_employee_model.py
tests/unit/models/test_discount_model.py
tests/unit/models/test_item_model.py
tests/unit/models/test_payment_type_model.py
tests/unit/models/test_receipt_model.py
tests/unit/models/test_pos_device_model.py</files>
  <action>
Run pytest on the test suite to verify all tests pass with zero failures and zero errors.

Execute: `cd /home/dagsdags/workspace/personal-projects/loyverse_sdk && python -m pytest tests/ -v`

The test run must exit with code 0 and show zero failures and zero errors across all test files.

If any test fails, analyze the failure and fix the underlying issue. Common failure modes after import fixes:
- Missing import: ensure all model classes are imported from the correct module
- Field name mismatch: verify field names match the Pydantic model definitions in src/loyverse_sdk/models/
- Any other failures: investigate and correct

After fixing any failures, re-run pytest until all tests pass.
  </action>
  <verify>
<automated>cd /home/dagsdags/workspace/personal-projects/loyverse_sdk && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pytest exits with code 0, zero failures, zero errors</done>
</task>

</tasks>

<verification>
1. grep confirms no `from loyverse_api` imports remain in any test file under tests/unit/models/
2. grep confirms `r.surcharge` (not `r.surchage`) in test_receipt_model.py
3. pytest runs with zero failures and zero errors
</verification>

<success_criteria>
- All 8 test files import from `loyverse_sdk` (not `loyverse_api`)
- Receipt model test uses `r.surcharge` (not `r.surchage`)
- pytest run completes with exit code 0, zero failures, zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-fixes-verification/02-01-SUMMARY.md`
</output>

---
phase: 01-code-cleanup-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loyverse_sdk/models/shift.py (delete)
  - src/loyverse_sdk/db/schemas.py (delete)
  - src/loyverse_sdk/core/logging.py (delete)
  - src/loyverse_sdk/utils.py (edit)
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04
user_setup: []

must_haves:
  truths:
    - "No orphaned files remain: models/shift.py is gone"
    - "No orphaned files remain: db/schemas.py is gone"
    - "No orphaned files remain: core/logging.py is gone"
    - "utils.py has no convert_response or use_model functions"
    - "standardize_datetime_str still works and is importable"
    - "The codebase imports cleanly (from loyverse_sdk import LoyverseClient succeeds)"
  artifacts:
    - path: "src/loyverse_sdk/utils.py"
      provides: "Clean utility module with only standardize_datetime_str"
      not_contains: ["def convert_response", "def use_model"]
    - path: "src/loyverse_sdk/models/shift.py"
      provides: "Deleted orphaned model"
      exists: false
    - path: "src/loyverse_sdk/db/schemas.py"
      provides: "Deleted dead SQLModel schema"
      exists: false
    - path: "src/loyverse_sdk/core/logging.py"
      provides: "Deleted stub"
      exists: false
  key_links:
    - from: "src/loyverse_sdk/utils.py"
      to: "src/loyverse_sdk/endpoints/mixins.py"
      via: "from loyverse_sdk.utils import standardize_datetime_str"
      pattern: "standardize_datetime_str"
    - from: "src/loyverse_sdk/db/exporter.py"
      to: "src/loyverse_sdk/db/schemas.py"
      via: "No import exists (verified)"
      pattern: "(no reference)"
    - from: "src/loyverse_sdk/models/__init__.py"
      to: "src/loyverse_sdk/models/shift.py"
      via: "No import exists (verified)"
      pattern: "(no reference)"
---

<objective>
Remove all orphaned files and dead code from the source tree.

**Purpose:** Eliminate dead code that creates noise, carries maintenance burden, and could cause `ModuleNotFoundError` for consumers unaware these modules are unused. Each deletion is self-contained — confirmed via pre-audit that no `__init__.py` or other module imports these files.

**Output:** Clean source tree with `shift.py`, `schemas.py`, `logging.py` removed and `utils.py` containing only `standardize_datetime_str`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-code-cleanup-bugfixes/01-CONTEXT.md
@.planning/phases/01-code-cleanup-bugfixes/01-PATTERNS.md

- Decisions D-06, D-08, D-09, D-10 apply to this plan
- Pre-audit confirmed: no `__init__.py` imports reference `shift.py`, `schemas.py`, or `logging.py`
- `standardize_datetime_str` is imported by `src/loyverse_sdk/endpoints/mixins.py:5` via `from loyverse_sdk.utils import standardize_datetime_str`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned models/shift.py</name>
  <files>
    <read_first>
      src/loyverse_sdk/models/shift.py
      src/loyverse_sdk/models/__init__.py
      src/loyverse_sdk/endpoints/__init__.py
    </read_first>
  </files>
  <action>
    Delete `src/loyverse_sdk/models/shift.py` entirely using `git rm` or `rm` followed by `git add`.

    Pre-deletion verification (already done in patterns audit — verify once more):
    - `src/loyverse_sdk/models/__init__.py` line 1-19: no `shift` import, no `Shift` or `ShiftListResponse` in imports
    - `src/loyverse_sdk/endpoints/__init__.py`: no `ShiftEndpoint` or similar reference (there is no shift endpoint class in the codebase)
    - `src/loyverse_sdk/client.py` line 51-64 (endpoint instantiation): no shift endpoint property
    
    Post-deletion: run `python -c "from loyverse_sdk import LoyverseClient"` to confirm no import breakage.
  </action>
  <verify>
    <automated>
      # File must not exist
      test ! -f src/loyverse_sdk/models/shift.py && echo "OK"
      # Import must still work
      python -c "from loyverse_sdk import LoyverseClient; print('OK')"
    </automated>
  </verify>
  <done>models/shift.py deleted, import smoke test passes</done>
  <acceptance_criteria>
    - `src/loyverse_sdk/models/shift.py` does not exist on disk
    - `python -c "from loyverse_sdk import LoyverseClient"` exits 0
    - `uv run pytest --collect-only` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete dead db/schemas.py and core/logging.py</name>
  <files>
    <read_first>
      src/loyverse_sdk/db/schemas.py
      src/loyverse_sdk/core/logging.py
      src/loyverse_sdk/db/__init__.py
      src/loyverse_sdk/db/exporter.py
      src/loyverse_sdk/core/__init__.py
    </read_first>
  </files>
  <action>
    Delete both files:

    1. Delete `src/loyverse_sdk/db/schemas.py` — this is a dead SQLModel schema file (lines 1-54) that defines `Employee`, `Customer`, `PaymentType`, `Discount`, `Receipt` SQLModel table classes. It references `config.db_url` which does not exist on the `Config` class (Config only has `BASE_URL`, `PAGE_LIMIT`, `TIMEZONE`). The export pipeline uses `schema_builder.py` raw SQL instead. Confirmed: `db/__init__.py` lines 1-47 has no `schemas` import; `exporter.py` lines 8-16 imports from `schema_builder.py` and `connection.py`, not `schemas.py`.

    2. Delete `src/loyverse_sdk/core/logging.py` — this is a stub file containing only `class Logger: ...` (line 1) with no body. Never imported anywhere in the codebase. Confirmed: `core/__init__.py` (if exists) has no logging import; no other module references `logging.Logger` or `from loyverse_sdk.core.logging import Logger`.

    Post-deletion: run `python -c "from loyverse_sdk import LoyverseClient"` and `uv run pytest --collect-only` to confirm no import breakage.
  </action>
  <verify>
    <automated>
      test ! -f src/loyverse_sdk/db/schemas.py && echo "schemas deleted"
      test ! -f src/loyverse_sdk/core/logging.py && echo "logging deleted"
      python -c "from loyverse_sdk import LoyverseClient; print('OK')"
    </automated>
  </verify>
  <done>schemas.py and logging.py deleted, import smoke test passes</done>
  <acceptance_criteria>
    - `src/loyverse_sdk/db/schemas.py` does not exist on disk
    - `src/loyverse_sdk/core/logging.py` does not exist on disk
    - `python -c "from loyverse_sdk import LoyverseClient"` exits 0
    - `uv run pytest --collect-only` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Clean up utils.py — remove dead convert_response and use_model</name>
  <files>
    <read_first>
      src/loyverse_sdk/utils.py
      src/loyverse_sdk/endpoints/mixins.py
    </read_first>
  </files>
  <action>
    Edit `src/loyverse_sdk/utils.py` (current 51 lines) to keep only `standardize_datetime_str`.

    Current file structure:
    - Line 1: `from datetime import datetime`
    - Line 2: `from functools import wraps`
    - Line 3: `from typing import Type, Callable, Any`
    - Line 4: `from pydantic import BaseModel`
    - Lines 7-10: `standardize_datetime_str(dt: datetime) -> str` (KEEP)
    - Lines 13-16: `convert_response(data, model)` (REMOVE)
    - Lines 19-51: `use_model(model)` decorator (REMOVE)

    Target file (should be exactly this after edit):
    ```python
    from datetime import datetime


    def standardize_datetime_str(dt: datetime) -> str:
        """Coverts datetime object to ISO 8601 format"""
        assert isinstance(dt, datetime), "dt must be a datetime object"
        return dt.isoformat(timespec="milliseconds") + "Z"
    ```

    Steps:
    1. Remove line 2 (`from functools import wraps`)
    2. Replace lines 3-4 (`from typing import Type, Callable, Any` and `from pydantic import BaseModel`) with a single blank line
    3. Remove everything from line 12 (blank) through line 51 (end) — this removes `convert_response` and `use_model`

    Post-edit: Run `python -c "from loyverse_sdk.utils import standardize_datetime_str; print(standardize_datetime_str.__doc__)"` to confirm the surviving function works.

    GREC gate: Do NOT use `grep -c standardize_datetime_str utils.py` to verify — `grep -c` counts comments and header prose. Use `grep -v '^#' | grep -c standardize_datetime_str` instead, expecting `== 1`.
  </action>
  <verify>
    <automated>
      python -c "from loyverse_sdk.utils import standardize_datetime_str; print('OK')"
      grep -n 'def convert_response\|def use_model' src/loyverse_sdk/utils.py; test $? -eq 1 && echo "dead funcs removed"
    </automated>
  </verify>
  <done>utils.py contains only standardize_datetime_str, dead funcs removed, standardize_datetime_str importable</done>
  <acceptance_criteria>
    - `src/loyverse_sdk/utils.py` contains exactly one function: `standardize_datetime_str`
    - `grep -n 'def convert_response\|def use_model' src/loyverse_sdk/utils.py` exits 1 (not found)
    - `grep 'from datetime import datetime' src/loyverse_sdk/utils.py` is present
    - `grep 'from functools' src/loyverse_sdk/utils.py` exits 1 (not found) — wraps import removed
    - `grep 'from typing\|from pydantic' src/loyverse_sdk/utils.py` exits 1 (not found)
    - `python -c "from loyverse_sdk.utils import standardize_datetime_str"` exits 0
    - `python -c "from loyverse_sdk.utils import convert_response"` raises `ImportError`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK source → import system | Deleted files could break module imports if references exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `models/shift.py` deletion | mitigate | Pre-audit confirmed no imports reference this file (D-09). Post-deletion `python -c "from loyverse_sdk import LoyverseClient"` smoke test (D-06). |
| T-01-02 | Tampering | `db/schemas.py` deletion | mitigate | Pre-audit confirmed no imports in `db/__init__.py`. File references non-existent `config.db_url` — would fail at runtime anyway. |
| T-01-03 | Tampering | `core/logging.py` deletion | mitigate | Pre-audit confirmed never imported anywhere in codebase. |
| T-01-04 | Tampering | `utils.py` function removal | mitigate | `grep` confirms `convert_response` and `use_model` have no callers outside utils.py. `standardize_datetime_str` is preserved and its only consumer is `mixins.py:5`. |
| T-01-SC | Tampering | Package installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
1. `python -c "from loyverse_sdk import LoyverseClient"` exits 0
2. `uv run pytest --collect-only` exits 0
3. `test ! -f src/loyverse_sdk/models/shift.py`
4. `test ! -f src/loyverse_sdk/db/schemas.py`
5. `test ! -f src/loyverse_sdk/core/logging.py`
6. `grep 'def convert_response\|def use_model' src/loyverse_sdk/utils.py` exits 1
</verification>

<success_criteria>
- All 3 orphaned files deleted and confirmed gone via `test ! -f`
- `utils.py` cleaned to only `standardize_datetime_str` (verified via grep)
- Import smoke test passes: `from loyverse_sdk import LoyverseClient` succeeds
- Test collection succeeds: `uv run pytest --collect-only` exits 0
</success_criteria>

<output>
Complete `.planning/phases/01-code-cleanup-bugfixes/01-01-SUMMARY.md` with outcome and verification results.
</output>
